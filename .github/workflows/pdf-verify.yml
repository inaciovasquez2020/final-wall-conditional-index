name: pdf-verify

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install TeX Live
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            texlive-latex-base \
            texlive-latex-recommended \
            texlive-latex-extra \
            texlive-fonts-recommended \
            latexmk

      - name: Build PDFs
        run: |
          latexmk -pdf -interaction=nonstopmode -halt-on-error Conditional_Final_Wall_Index.tex
          latexmk -pdf -interaction=nonstopmode -halt-on-error PROGRAM_STATUS.tex

